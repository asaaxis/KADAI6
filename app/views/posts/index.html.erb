<h1>一覧表示</h1>
<table>
  <tr>
    <th>内容</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
  </tr>
  <% end %>
  </table>
  <%= link_to 'ブログ一覧画面に戻る', posts_path %>
  <%= link_to '新しくブログを投稿する', new_post_path %>