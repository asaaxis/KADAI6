<div class = "header">
<h1>一覧表示</h1>

<p><%= notice %></p>
</div>

<div class = "body">
<table>
  <tr>
    <th>内容</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
    <td><%= link_to "つぶやきを編集する", edit_post_path(post.id), data: { confirm: '本当に編集していいですか？' }%></td>
    <td><%= link_to 'つぶやきを削除する', post_path(post.id), method: :delete, data: { confirm: '本当に削除していいですか？' }%></td>
  </tr>
  <% end %>
  </table>
  
  <%= link_to '新しくつぶやきを投稿する', new_post_path %>
</div>